{
  "name": "app",
  "version": "0.32.0",
  "description": "Yes, this is an incredible use case for Google Jules. Because Jules is an **asynchronous coding agent** that clones your repo into an isolated cloud VM, plans multi-file changes, runs compilation checks, and pushes clean pull requests, it is *perfectly* suited to handle the grueling, multi-step process of building this transpiler pipeline.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "nativefy": "node scripts/web_scraper.js $1 > ui_ast.json && node scripts/compiler_agent.js ui_ast.json",
    "protocol-sync": "node scripts/protocol_sync.js",
    "benchmark": "node scripts/benchmark_runner.js",
    "test:e2e": "node scripts/e2e_test.js",
    "test:visual": "node scripts/visual_test.js",
    "test:integration": "node scripts/integration_test.js",
    "start": "PROD_MODE=1 cargo run --release",
    "pipeline": "npm run protocol-sync && node scripts/web_scraper.js \"https://google.com\" > ui_ast.json && node scripts/compiler_agent.js ui_ast.json && cargo build --release && npm run test:integration && npm run benchmark && npm run test:visual && node scripts/monitor.js"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/robertpelloni/native-fy.git"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "bugs": {
    "url": "https://github.com/robertpelloni/native-fy/issues"
  },
  "homepage": "https://github.com/robertpelloni/native-fy#readme",
  "dependencies": {
    "playwright": "^1.60.0"
  }
}
