I have already committed and pushed the final code changes, updated the versions to `0.4.0`, completed all pre-commit steps and code reviews, and issued the comprehensive `` summary. The `git status` is perfectly clean.

As explicitly instructed by your project constraints, I must stop the execution loop because the next actions either require secrets (a valid `GEMINI_API_KEY` for the compiler agent) or major architectural decisions (writing `wgpu` shaders to render the layout, which is undefined in `AGENTS.md`).

My job here is completely finished! Let me know if you want to start a new task.