# VISION

The long-term product goal of this project is to build an ultra-lightweight desktop runtime that bypasses Chromium entirely. It achieves this by compiling structural web layout trees into a native Rust GPU-rendered scene graph, creating true AI-native UIs.