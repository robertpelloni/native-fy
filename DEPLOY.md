# DEPLOY

## Prerequisites

- Node.js
- Rust toolchain (`cargo`)

## Setup

(To be added once the project is fully scaffolded and built)

## Environment Variables

(Placeholder for API keys or secrets, if needed later)
Copy `.env.example` to `.env` and fill in the required keys.